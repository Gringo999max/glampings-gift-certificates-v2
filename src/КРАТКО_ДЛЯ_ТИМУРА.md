# 🚀 КРАТКО: Что нужно сделать Тимуру

## Задача
Добавить страницу "Корпоративные подарки" на сайт **glamping.rf**

---

## 1️⃣ СКОПИРОВАТЬ ФАЙЛЫ

### Основные компоненты (26 файлов)

**B2B Страница:**
```
/components/CorporateB2BPage.tsx
/components/B2BCertificateHeroVariants.tsx
/components/B2BPackagesByPriceV2Variants.tsx
/components/BusinessResultsSection.tsx
/components/CorporateGiftReasonsSectionVariants.tsx
/components/HowItWorksB2BVariants.tsx
/components/HRDashboardShowcaseVariants.tsx
/components/CorporateB2BFinalCTAVariants.tsx
/components/CorporateRequestFormModal.tsx
/components/FoxIllustrations.tsx
```

**HR Dashboard (6 файлов):**
```
/components/HRDashboardPage.tsx
/components/DashboardTab.tsx
/components/CertificatesTab.tsx
/components/AnalyticsTab.tsx
/components/ReportsTab.tsx
/components/SettingsTab.tsx
```

**Popover (6 файлов):**
```
/components/CorporatePopover.tsx
/components/CertificatesPopover.tsx
/components/DeliveryPopover.tsx
/components/ReviewsPopover.tsx
/components/HowItWorksPopover.tsx
/components/AboutPopover.tsx
```

**Header + Footer:**
```
/components/Header.tsx
/components/Footer.tsx
```

**Утилиты:**
```
/components/ui/* (вся папка)
/components/figma/ImageWithFallback.tsx
/components/utils/orderUtils.ts
/styles/globals.css
```

---

## 2️⃣ НАСТРОИТЬ ПОДДОМЕН

**URL:** `b2b.glamping.rf` или `corporate.glamping.rf`

**Что сделать:**
1. Создать A-запись в DNS
2. Настроить SSL-сертификат
3. Настроить роутинг на CorporateB2BPage

---

## 3️⃣ TELEGRAM-БОТ ДЛЯ ЗАЯВОК

### Шаг 1: Создать бота

```
1. Открыть @BotFather в Telegram
2. /newbot
3. Название: "Glamping B2B Заявки"
4. Получить токен
5. Создать канал для заявок
6. Добавить бота в канал
7. Получить chat_id
```

### Шаг 2: Добавить в .env

```env
TELEGRAM_BOT_TOKEN=твой_токен
TELEGRAM_CHAT_ID=твой_chat_id
```

### Шаг 3: Создать API endpoint

**Файл:** `/api/send-b2b-request.js`

```javascript
const TelegramBot = require('node-telegram-bot-api');
const bot = new TelegramBot(process.env.TELEGRAM_BOT_TOKEN);

export default async function handler(req, res) {
  const { companyName, contactPerson, email, phone, employeeCount, packageInterest, timeline, comments } = req.body;

  const message = `
🎁 НОВАЯ ЗАЯВКА B2B

🏢 Компания: ${companyName}
👤 Контакт: ${contactPerson}
📧 Email: ${email}
📱 Телефон: ${phone}
👥 Сотрудников: ${employeeCount}
📦 Пакет: ${packageInterest}
📅 Сроки: ${timeline}
💬 Комментарии: ${comments}
  `;

  await bot.sendMessage(process.env.TELEGRAM_CHAT_ID, message, { parse_mode: 'HTML' });
  res.status(200).json({ success: true });
}
```

### Шаг 4: Обновить форму

**Файл:** `/components/CorporateRequestFormModal.tsx`

Найти `handleSubmit` и заменить на:

```typescript
const handleSubmit = async (e) => {
  e.preventDefault()
  setIsSubmitting(true)

  const response = await fetch('/api/send-b2b-request', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(formData),
  })

  if (response.ok) {
    setShowSuccessDialog(true)
  }
  setIsSubmitting(false)
}
```

---

## 4️⃣ ПОДГОТОВИТЬ МЕСТО ДЛЯ ФОТО

**Для Натальи:**

Создать папку:
```
/public/images/packaging/
```

Наталья загрузит туда фото конвертов и упаковок:
- envelope-front.jpg
- envelope-open.jpg  
- box-closed.jpg
- box-open.jpg
- premium-full.jpg
- premium-details.jpg

**После загрузки фото - обновить пути** в:
- `/components/DeliveryPaymentPage.tsx`
- `/components/PackagingCarousel.tsx`

---

## ✅ ЧЕКЛИСТ

- [ ] Скопировать все файлы из списка
- [ ] Настроить поддомен b2b.glamping.rf
- [ ] Создать Telegram-бота
- [ ] Создать API /api/send-b2b-request
- [ ] Обновить форму CorporateRequestFormModal
- [ ] Создать папку /public/images/packaging/
- [ ] Протестировать отправку заявки
- [ ] Проверить на мобильных
- [ ] Задеплоить

---

## ⏰ ВРЕМЯ

**Всего:** 2-3 рабочих дня

---

## 📞 ВОПРОСЫ?

Смотри полное ТЗ в файле: `ТЕХНИЧЕСКОЕ_ЗАДАНИЕ_ДЛЯ_ТИМУРА.md`
