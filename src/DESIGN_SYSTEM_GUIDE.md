# 🎨 Дизайн-система глэмпинги.рф

## 📖 Общая философия дизайна

**Источник вдохновения:** Сайт Cuva - чистый, современный дизайн-подход с акцентом на природную тематику.

**Ключевые принципы:**
- ✨ **Минимализм и чистота** - много воздуха, просторные макеты
- 🌿 **Природная эстетика** - зеленые оттенки, естественные образы, мягкие формы
- 🎯 **Ориентация на действие** - четкие CTA, понятная навигация
- 📱 **Mobile-first подход** - все компоненты адаптивны
- 🎭 **Микроанимации** - плавные transitions, hover-эффекты, появления секций
- 🦊 **Дружелюбность** - иллюстрации лисичек, теплые тексты, человечный tone of voice

---

## 🎨 Цветовая палитра

### Основные цвета (Primary)

**Зеленый (Emerald) - главный бренд-цвет:**
```css
/* Основной зеленый */
--emerald-50: #f0fdf4    /* Очень светлый фон */
--emerald-100: #dcfce7   /* Светлый фон, badges */
--emerald-200: #bbf7d0   /* Hover states, borders */
--emerald-400: #4ade80   /* Акценты, иконки */
--emerald-500: #22c55e   /* Основные кнопки */
--emerald-600: #16a34a   /* Primary green, кнопки */
--emerald-700: #15803d   /* Hover на кнопках */

/* Специальный корпоративный зеленый */
--corporate-green: #007a55  /* Темнее, для корпоративных страниц */
--corporate-green-hover: #006644
```

**Teal (Бирюзовый) - дополнительный:**
```css
--teal-50: #f0fdfa      /* Градиенты, фоны */
--teal-100: #ccfbf1     /* Акцентные элементы */
```

### Нейтральные цвета (Neutrals)

**Gray - для текстов и фонов:**
```css
--gray-50: #f9fafb      /* Светлый фон секций */
--gray-100: #f3f4f6     /* Альтернативный фон */
--gray-200: #e5e7eb     /* Borders, разделители */
--gray-300: #d1d5db     /* Неактивные элементы */
--gray-400: #9ca3af     /* Вторичный текст */
--gray-600: #4b5563     /* Основной текст */
--gray-700: #374151     /* Темный текст */
--gray-900: #111827     /* Заголовки, темные секции */
```

### Акцентные цвета

**Amber (Янтарный) - для тепла и природы:**
```css
--amber-50: #fffbeb     /* Фоны для изображений */
--amber-100: #fef3c7    /* Акценты, highlights */
--amber-400: #fbbf24    /* Звезды в рейтингах */
```

**Red (Красный) - для ошибок:**
```css
--red-500: #ef4444      /* Ошибки */
--red-600: #dc2626      /* Hover на ошибках */
--red-50: #fef2f2       /* Фон ошибок */
```

### Градиенты

**Часто используемые градиенты:**
```css
/* Hero секции */
background: linear-gradient(to bottom right, #f0fdf4, #f0fdfa);
/* from-emerald-50 to-teal-50 */

/* Акцентные секции */
background: linear-gradient(to right, #f0fdf4, #f0fdfa);
/* from-emerald-50 to-teal-50 */

/* Темные секции */
background: linear-gradient(to bottom, #111827, #1f2937);
/* from-gray-900 to-gray-800 */
```

---

## 📝 Типографика

### Шрифты

**Основной шрифт:** System UI font stack (по умолчанию Tailwind)
```css
font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
```

### Размеры и стили текста

**Заголовки (Headings):**
```css
/* H1 - Hero заголовки */
font-size: 3rem;        /* 48px, text-5xl */
font-weight: 700;       /* font-bold */
line-height: 1.2;       /* leading-tight */
color: #111827;         /* text-gray-900 */

/* Desktop: 3.75rem (60px, text-6xl) */

/* H2 - Заголовки секций */
font-size: 2.25rem;     /* 36px, text-4xl */
font-weight: 700;       /* font-bold */
line-height: 1.3;
color: #111827;

/* H3 - Подзаголовки */
font-size: 1.5rem;      /* 24px, text-2xl */
font-weight: 600;       /* font-semibold */
line-height: 1.4;
color: #111827;

/* H4 - Карточки */
font-size: 1.125rem;    /* 18px, text-lg */
font-weight: 600;
line-height: 1.5;
color: #111827;
```

**Body текст:**
```css
/* Основной текст */
font-size: 1rem;        /* 16px, text-base */
font-weight: 400;       /* font-normal */
line-height: 1.5;       /* leading-normal */
color: #4b5563;         /* text-gray-600 */

/* Крупный текст (Lead) */
font-size: 1.25rem;     /* 20px, text-xl */
line-height: 1.75;      /* leading-relaxed */
color: #374151;         /* text-gray-700 */

/* Мелкий текст */
font-size: 0.875rem;    /* 14px, text-sm */
line-height: 1.5;
color: #6b7280;         /* text-gray-500 */

/* Очень мелкий */
font-size: 0.75rem;     /* 12px, text-xs */
line-height: 1.4;
color: #9ca3af;         /* text-gray-400 */
```

**Специальные стили:**
```css
/* TypewriterText эффект */
color: #16a34a;         /* text-emerald-600 */
font-weight: 700;       /* font-bold */
/* Анимация печатающегося текста */

/* Акцентный текст */
font-weight: 600;       /* font-semibold */
color: #16a34a;         /* text-emerald-600 */
```

---

## 🔘 Компоненты

### Кнопки (Buttons)

**Primary Button (Главная):**
```css
background: #16a34a;      /* bg-emerald-600 */
color: white;
padding: 1rem 2rem;       /* px-8 py-4 */
border-radius: 9999px;    /* rounded-full */
font-weight: 600;         /* font-semibold */
font-size: 1rem;          /* text-base */
box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1); /* shadow-lg */
transition: all 200ms;

/* Hover */
background: #15803d;      /* hover:bg-emerald-700 */
box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1); /* hover:shadow-xl */
transform: scale(1.05);   /* hover:scale-105 */
```

**Corporate Button (Для корпоративных страниц):**
```css
background: #007a55;
color: white;
/* Остальное как Primary */

/* Hover */
background: #006644;
```

**Secondary Button (Вторичная):**
```css
background: transparent;
color: #16a34a;           /* text-emerald-600 */
border: 2px solid #16a34a; /* border-2 border-emerald-600 */
padding: 1rem 2rem;
border-radius: 9999px;

/* Hover */
background: #f0fdf4;      /* hover:bg-emerald-50 */
```

**Ghost Button (Прозрачная):**
```css
background: transparent;
color: #4b5563;           /* text-gray-600 */
padding: 0.5rem 1rem;

/* Hover */
background: #f3f4f6;      /* hover:bg-gray-100 */
```

**Размеры кнопок:**
```css
/* Small */
padding: 0.5rem 1rem;     /* px-4 py-2 */
font-size: 0.875rem;      /* text-sm */

/* Medium (default) */
padding: 0.75rem 1.5rem;  /* px-6 py-3 */
font-size: 1rem;          /* text-base */

/* Large */
padding: 1rem 2rem;       /* px-8 py-4 */
font-size: 1.125rem;      /* text-lg */
```

### Карточки (Cards)

**Стандартная карточка:**
```css
background: white;
border-radius: 1.5rem;    /* rounded-3xl */
padding: 2rem;            /* p-8 */
border: 2px solid #f3f4f6; /* border-2 border-gray-100 */
box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1); /* shadow-lg */
transition: all 300ms;

/* Hover */
border-color: #bbf7d0;    /* hover:border-emerald-200 */
box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1); /* hover:shadow-2xl */
transform: translateY(-8px); /* hover:-translate-y-2 */
```

**Компактная карточка (мобильная):**
```css
background: white;
border-radius: 0.75rem;   /* rounded-lg */
padding: 0.75rem;         /* p-3 */
border: 1px solid #e5e7eb; /* border border-gray-200 */
box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1); /* shadow-sm */
```

**Карточка сертификата:**
```css
background: white;
border-radius: 1.5rem;
padding: 1.5rem;
border: 2px solid #f3f4f6;
/* Изображение сверху */
/* Контент внизу */
/* Badge с ценой */
```

### Бейджи (Badges)

**Числовой бейдж (например, номер шага):**
```css
width: 3rem;              /* w-12 */
height: 3rem;             /* h-12 */
background: #16a34a;      /* bg-emerald-500 */
color: white;
border-radius: 9999px;    /* rounded-full */
display: flex;
align-items: center;
justify-content: center;
font-weight: 700;         /* font-bold */
font-size: 1.125rem;      /* text-lg */
box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
position: absolute;
top: -0.75rem;            /* -top-3 */
left: -0.75rem;           /* -left-3 */
```

**Текстовый бейдж (например, "Для бизнеса"):**
```css
background: #dcfce7;      /* bg-emerald-100 */
color: #15803d;           /* text-emerald-700 */
padding: 0.5rem 1rem;     /* px-4 py-2 */
border-radius: 9999px;    /* rounded-full */
font-size: 0.875rem;      /* text-sm */
font-weight: 500;         /* font-medium */
display: inline-flex;
align-items: center;
gap: 0.5rem;
```

### Инпуты (Form Inputs)

**Текстовое поле:**
```css
background: white;
border: 2px solid #e5e7eb; /* border-2 border-gray-200 */
border-radius: 0.75rem;    /* rounded-xl */
padding: 0.75rem 1rem;     /* px-4 py-3 */
font-size: 1rem;
color: #111827;
transition: all 200ms;

/* Focus */
border-color: #16a34a;     /* focus:border-emerald-600 */
outline: none;
box-shadow: 0 0 0 3px rgba(22, 163, 74, 0.1); /* focus:ring */

/* Error */
border-color: #ef4444;     /* border-red-500 */
background: #fef2f2;       /* bg-red-50 */
```

**Textarea:**
```css
/* То же что инпут, но: */
min-height: 8rem;          /* rows={4} */
resize: vertical;
```

**Label:**
```css
font-size: 0.875rem;       /* text-sm */
font-weight: 500;          /* font-medium */
color: #374151;            /* text-gray-700 */
margin-bottom: 0.5rem;     /* mb-2 */
display: block;
```

---

## 📐 Spacing & Layout

### Отступы (Padding)

**Секции:**
```css
/* Вертикальные отступы секций */
padding-top: 4rem;         /* py-16 */
padding-bottom: 4rem;

/* Desktop */
padding-top: 6rem;         /* lg:py-24 */
padding-bottom: 6rem;
```

**Контейнеры:**
```css
max-width: 80rem;          /* max-w-7xl (1280px) */
margin: 0 auto;            /* mx-auto */
padding-left: 1.5rem;      /* px-6 */
padding-right: 1.5rem;
```

**Карточки:**
```css
/* Крупные карточки */
padding: 2rem;             /* p-8 */

/* Средние */
padding: 1.5rem;           /* p-6 */

/* Компактные */
padding: 1rem;             /* p-4 */
```

### Gaps (Промежутки)

**Грид:**
```css
/* Между карточками */
gap: 2rem;                 /* gap-8 */

/* На мобильных */
gap: 1rem;                 /* gap-4 */
```

**Flex:**
```css
/* Между элементами */
gap: 1rem;                 /* gap-4 */
```

### Размеры элементов

**Изображения в карточках:**
```css
/* Desktop */
height: 12rem;             /* h-48 (192px) */

/* Mobile */
height: 8rem;              /* h-32 (128px) */

/* Компактная мобильная */
height: 4rem;              /* h-16 (64px) */
```

---

## 🎭 Анимации и переходы

### Transitions

**Стандартный transition:**
```css
transition: all 200ms ease-in-out;
/* transition-all duration-200 */
```

**Для сложных hover-эффектов:**
```css
transition: all 300ms ease-in-out;
/* transition-all duration-300 */
```

**Для изображений:**
```css
transition: transform 500ms ease-in-out;
/* transition-transform duration-500 */
```

### Hover эффекты

**Карточки:**
```css
/* Scale */
transform: translateY(-8px);
/* hover:-translate-y-2 */

/* Shadow */
box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
/* hover:shadow-2xl */

/* Border color */
border-color: #bbf7d0;
/* hover:border-emerald-200 */
```

**Изображения:**
```css
transform: scale(1.05);
/* group-hover:scale-105 */
```

**Кнопки:**
```css
transform: scale(1.05);
/* hover:scale-105 */

/* Active */
transform: scale(0.95);
/* active:scale-95 */
```

### Анимации появления (Motion)

**Fade in:**
```jsx
initial={{ opacity: 0, y: 20 }}
animate={{ opacity: 1, y: 0 }}
transition={{ duration: 0.6 }}
```

**Секции с задержкой:**
```jsx
initial={{ opacity: 0, y: 30 }}
animate={{ opacity: 1, y: 0 }}
transition={{ duration: 0.6, delay: index * 0.2 }}
```

**Hover анимация:**
```jsx
whileHover={{ y: -8 }}
```

### Специальные анимации

**TypewriterText:**
- Печатающийся текст в Hero
- Циклическое переключение слов
- Плавное появление/исчезание

**Carousel:**
- Автопереключение каждую 1 секунду
- Hover-превью с увеличением
- Плавные переходы

---

## 🎨 Специфичные паттерны

### Hero секции

**Структура:**
```
┌─────────────────────────────────────┐
│  [Badge: категория]                 │
│  H1: Заголовок с TypewriterText     │
│  P: Описание (text-xl)              │
│  [Primary Button] [Secondary Button]│
│  Trust indicators (CheckCircle)     │
├─────────────────────────────────────┤
│  [Hero изображение с декорациями]   │
└─────────────────────────────────────┘
```

**Фон:**
```css
background: linear-gradient(to bottom right, #f0fdf4, #f0fdfa);
```

### Секция "Как это работает"

**Карточки шагов:**
- Числовой бейдж слева вверху
- Изображение (или иллюстрация)
- Заголовок
- Описание
- Стрелка между шагами (→)

**Desktop:** grid-cols-3 или grid-cols-4
**Mobile:** grid-cols-1

### Карточки сертификатов

**Структура:**
```
┌───────────────────────┐
│   [Изображение]       │
│   [Badge: цена]       │
├───────────────────────┤
│   Название            │
│   Описание            │
│   [Кнопка выбора]     │
└───────────────────────┘
```

### Формы

**Checkout Modal:**
- Полноэкранная страница (не попап!)
- Шаги слева (на desktop)
- Форма справа
- Валидация с красными рамками
- Автоформат телефона
- Умная кнопка "Оплатить"
- Автоскролл к ошибкам

### Модальные окна

**Desktop:** Центрированный modal с overlay
**Mobile:** Drawer снизу вверх ИЛИ кастомное мода��ьное окно

**Структура мобильного модала:**
```
┌───────────────────┐
│  [Полоска свайпа] │
│  Header + [X]     │
├───────────────────┤
│  [Прокрутка]      │
│   Контент         │
│                   │
├───────────────────┤
│  [Sticky footer]  │
│  [CTA кнопка]     │
└───────────────────┘
```

---

## 🦊 Иллюстрации и графика

### Стиль иллюстраций

**Лисичка-талисман:**
- Простые SVG иллюстрации
- Оранжевый лис (#f97316, orange-500)
- Минималистичный стиль
- 4 состояния: дарит подарок, выбирает, бронирует, в глэмпинге

**Где используются:**
- Корпоративные страницы (шаги)
- Секция "Как это работает"
- Пустые состояния

### Изображения

**Источник:** Unsplash через unsplash_tool

**Стиль фотографий:**
- Естественное освещение
- Природные пейзажи
- Глэмпинги, шатры, домики
- Счастливые люди
- Теплые тона

**Обработка:**
```css
border-radius: 1.5rem;     /* rounded-2xl */
object-fit: cover;
/* Hover: scale(1.05) */
```

### Иконки

**Библиотека:** lucide-react

**Часто используемые:**
- CheckCircle (галочки)
- ArrowRight (стрелки)
- Gift (подарок)
- Building2 (корпоративное)
- Phone, Mail (контакты)
- X (закрытие)

**Размеры:**
```css
/* Маленькие */
width: 1rem; height: 1rem;     /* w-4 h-4 */

/* Средние */
width: 1.25rem; height: 1.25rem; /* w-5 h-5 */

/* Крупные */
width: 2rem; height: 2rem;     /* w-8 h-8 */
```

---

## 📱 Мобильная адаптивность

### Breakpoints

```css
/* Mobile first подход */
/* Default: mobile (< 640px) */

sm: 640px;   /* Маленькие планшеты */
md: 768px;   /* Планшеты */
lg: 1024px;  /* Десктоп */
xl: 1280px;  /* Большие экраны */
```

### Адаптивные паттерны

**Грид:**
```css
/* Mobile */
grid-cols-1

/* Tablet */
md:grid-cols-2

/* Desktop */
lg:grid-cols-3 или lg:grid-cols-4
```

**Текст:**
```css
/* H1 */
text-4xl md:text-5xl lg:text-6xl

/* Body */
text-base lg:text-lg
```

**Spacing:**
```css
/* Padding секций */
py-12 lg:py-24

/* Margins */
mb-6 lg:mb-12
```

**Hide/Show:**
```css
/* Скрыть на мобильных */
hidden md:block

/* Показать только на мобильных */
block md:hidden
```

### Мобильные особенности

**Navigation:**
- Бургер-меню на мобильных
- Полноэкранное меню

**Модалы:**
- Drawer снизу вверх ИЛИ
- Кастомное окно с фиксированным footer

**Формы:**
- Вертикальный layout
- Полная ширина кнопок
- Увеличенные touch targets

**Images:**
- Aspect ratio: 16:9 или 4:3
- Меньшая высота

---

## 🎯 UX паттерны

### Навигация

**Header:**
- Логотип слева
- Меню по центру (desktop)
- CTA кнопка справа
- Sticky на скролле

**Footer:**
- 4 колонки (desktop)
- 1 колонка (mobile)
- Логотип, ссылки, контакты, соцсети

### Call-to-Action

**Иерархия:**
1. Primary button - главное действие
2. Secondary button - альтернатива
3. Text link - дополнительная информация

**Расположение:**
- В конце секций
- В Hero
- В карточках
- Sticky footer (мобильные формы)

### Интерактивность

**Hover states:**
- Всегда на desktop
- Никогда на мобильных (используй active)

**Loading states:**
- Скелетоны для карточек
- Спиннеры для кнопок

**Error states:**
- Красная рамка на инпутах
- Текст ошибки под полем
- Иконка ошибки

### Валидация форм

**Правила:**
- Валидация при submit
- Показ ошибок под полями
- Автоскролл к первой ошибке
- Красная рамка на ошибочных полях
- Зеленая галочка на валидных

**Автоформатирование:**
- Телефон: +7 (XXX) XXX-XX-XX
- Сумма: разделители тысяч

---

## 🔧 Технические детали

### Используемые библиотеки

**UI компоненты:**
- shadcn/ui из `/components/ui`
- Tailwind CSS v4.0
- lucide-react (иконки)

**Анимации:**
- motion/react (Framer Motion)

**Формы:**
- react-hook-form@7.55.0

**Карусели:**
- react-slick
- Кастомные компоненты (PackagingCarousel, EnvelopeCarousel)

### Кастомные компоненты

**TypewriterText:**
- Печатающийся эффект
- Циклическое переключение слов
- `<TypewriterText words={['слово1', 'слово2']} className="text-emerald-600" />`

**ImageWithFallback:**
- Защищенный компонент
- НЕ МОДИФИЦИРОВАТЬ!
- `import { ImageWithFallback } from './components/figma/ImageWithFallback'`

**PackagingCarousel / EnvelopeCarousel:**
- Автопереключение каждую 1 секунду
- Hover-превью с увеличением
- Мгновенный выбор без кнопки

### Правила именования

**Файлы:**
- PascalCase для компонентов: `HeroSection.tsx`
- camelCase для утилит: `orderUtils.ts`

**Классы:**
- Tailwind утилиты
- Никаких кастомных CSS классов (кроме globals.css)

**Переменные:**
- camelCase: `isOpen`, `formData`
- UPPERCASE для констант: `MAX_ITEMS`

---

## 📋 Чеклист для новых страниц

При создании новой страницы, убедись что:

- [ ] Используется зеленая цветовая схема (emerald)
- [ ] Hero секция с градиентным фоном
- [ ] TypewriterText в заголовке (где уместно)
- [ ] Все карточки с rounded-3xl, border-2, shadow-lg
- [ ] Hover эффекты: -translate-y-2, scale-105, shadow-2xl
- [ ] Мобильная адаптивность: grid-cols-1 md:grid-cols-2 lg:grid-cols-3
- [ ] Motion анимации для секций (fade in, delay)
- [ ] CTA кнопки: rounded-full, px-8 py-4, emerald-600
- [ ] Spacing: py-16 для секций, gap-8 для грида
- [ ] Иконки из lucide-react
- [ ] Изображения через unsplash_tool
- [ ] Footer компонент внизу
- [ ] Header компонент вверху
- [ ] Формы с валидацией и автоформатом телефона
- [ ] Модалы адаптивны (Drawer на мобильных)

---

## 🎨 Примеры кода

### Hero секция (шаблон)

```tsx
<section className="relative bg-gradient-to-br from-emerald-50 to-teal-50 py-16 lg:py-24 overflow-hidden">
  <div className="max-w-7xl mx-auto px-6">
    <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <div>
        <div className="inline-flex items-center gap-2 bg-emerald-100 text-emerald-700 px-4 py-2 rounded-full mb-6">
          <Icon className="w-4 h-4" />
          <span className="text-sm font-medium">Категория</span>
        </div>
        
        <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 mb-6 leading-tight">
          Заголовок <TypewriterText words={['слово1', 'слово2']} className="text-emerald-600" />
        </h1>
        
        <p className="text-xl text-gray-700 mb-8 leading-relaxed">
          Описание секции
        </p>
        
        <div className="flex flex-col sm:flex-row gap-4">
          <Button size="lg" className="bg-emerald-600 hover:bg-emerald-700">
            Главное действие
          </Button>
          <Button variant="outline" size="lg" className="border-emerald-600 text-emerald-600">
            Альтернатива
          </Button>
        </div>
      </div>
      
      <div className="relative">
        <ImageWithFallback
          src={imageUrl}
          alt="Описание"
          className="w-full h-96 object-cover rounded-2xl shadow-2xl"
        />
      </div>
    </div>
  </div>
</section>
```

### Карточка с анимацией (шаблон)

```tsx
<motion.div
  className="relative bg-white rounded-3xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300 border-2 border-gray-100 hover:border-emerald-200 group"
  initial={{ opacity: 0, y: 30 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.6, delay: index * 0.2 }}
  whileHover={{ y: -8 }}
>
  {/* Badge */}
  <div className="absolute -top-3 -left-3 w-12 h-12 bg-emerald-500 text-white rounded-full flex items-center justify-center shadow-lg">
    <span className="text-xl font-bold">1</span>
  </div>
  
  {/* Image */}
  <div className="relative w-full h-48 mb-6 rounded-2xl overflow-hidden bg-amber-50">
    <ImageWithFallback
      src={image}
      alt="Описание"
      className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
    />
  </div>
  
  {/* Content */}
  <h3 className="text-xl font-semibold mb-3 text-gray-900">
    Заголовок
  </h3>
  <p className="text-gray-600 leading-relaxed">
    Описание
  </p>
</motion.div>
```

### Форма с валидацией (шаблон)

```tsx
const [formData, setFormData] = useState({ name: '', email: '', phone: '' })
const [errors, setErrors] = useState<Record<string, string>>({})

const formatPhone = (value: string) => {
  const cleaned = value.replace(/\D/g, '').slice(0, 11)
  if (cleaned.length === 0) return ''
  
  let formatted = '+7'
  if (cleaned.length > 1) {
    formatted += ' (' + cleaned.slice(1, 4)
    if (cleaned.length > 4) {
      formatted += ') ' + cleaned.slice(4, 7)
      if (cleaned.length > 7) {
        formatted += '-' + cleaned.slice(7, 9)
        if (cleaned.length > 9) {
          formatted += '-' + cleaned.slice(9, 11)
        }
      }
    }
  }
  return formatted
}

<Input
  type="tel"
  value={formData.phone}
  onChange={(e) => setFormData(prev => ({ 
    ...prev, 
    phone: formatPhone(e.target.value) 
  }))}
  className={errors.phone ? 'border-red-500 bg-red-50' : ''}
  placeholder="+7 (XXX) XXX-XX-XX"
/>
{errors.phone && (
  <p className="text-red-500 text-sm mt-1">{errors.phone}</p>
)}
```

---

## 🎤 Tone of Voice

**Общий стиль текстов:**
- 🌟 **Дружелюбный** - обращение на "вы", теплые формулировки
- 🎯 **Конкретный** - четкие призывы к действию, понятные преимущества
- 🌿 **Природный** - метафоры о природе, отдыхе, восстановлении
- ✨ **Вдохновляющий** - акцент на впечатлениях, эмоциях

**Примеры:**
- ✅ "Подарите отдых на природе"
- ✅ "Незабываемые впечатления ждут"
- ✅ "Выбирайте из 500+ глэмпингов"
- ❌ "Купите сертификат"
- ❌ "Оформите заказ сейчас"

---

## 📦 Состояния страниц

### Завершенные страницы ✅
- **Главная** (HomePage) - полностью готова
- **Доставка** (DeliveryPaymentPage) - предварительно готова
- **Оплата** (в составе Delivery) - предварительно готова
- **Отзывы** (ReviewsPage) - предварительно готова
- **Как это работает** (HowItWorksPage) - предварительно готова

### В работе 🔧
- **Корпоративные подарки** (CorporatePage) - требует доработки
- **Активация сертификата** (CertificateActivationPage)

### Нужно создать 📝
- **О нас** (AboutPage) - частично есть
- **Pet-friendly глэмпинги** (PetFriendlyGlampingPage) - частично есть
- Любые новые страницы в стиле глэмпинги.рф

---

## 🚀 Быстрый старт для Figma Make

**Когда создаешь новый дизайн в Figma Make, используй этот промпт:**

```
Создай страницу [название] для сайта глэмпинги.рф в стиле Cuva.

Дизайн-система:
- Цвета: emerald-600 (#16a34a) как основной, градиенты from-emerald-50 to-teal-50
- Типографика: жирные заголовки (text-5xl font-bold), body text-xl
- Компоненты: карточки с rounded-3xl, кнопки rounded-full, тени shadow-lg
- Spacing: секции py-16, grid gap-8
- Анимации: hover:-translate-y-2, hover:scale-105
- Мобильная: grid-cols-1 md:grid-cols-2 lg:grid-cols-3

Обязательно:
- Hero с градиентным фоном
- TypewriterText эффект в заголовке
- Motion анимации для секций
- Зеленые CTA кнопки
- Изображения из Unsplash (природа, глэмпинги)
- Иконки из lucide-react
- Footer и Header компоненты

Стиль: чистый, минималистичный, природный, дружелюбный
```

---

**Этот гайд - твой полный справочник по дизайн-системе глэмпинги.рф! 🎨**

Используй его при создании любых новых страниц или компонентов.
