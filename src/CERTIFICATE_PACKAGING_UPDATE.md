# 🎁 Обновление блока выбора упаковки сертификата

## 📅 Дата: 3 октября 2025

---

## 🎯 Цель

Переработать блок "Добавьте к заказу подарочную упаковку" на странице сертификата с добавлением автоматических каруселей для выбора упаковки и конверта.

---

## ✅ Что было сделано

### 1. **Созданы отдельные компоненты карусели**

#### `/components/PackagingCarousel.tsx`
```typescript
interface PackagingCarouselProps {
  options: PackagingOption[]
  selectedId: string | null
  onSelect: (id: string) => void
}
```

**Функции:**
- ✅ Показывает 3 варианта упаковки одновременно
- ✅ Автоматическое переключение каждую **1 секунду**
- ✅ Плавная анимация сдвига влево (duration: 1000ms)
- ✅ Бесконечный цикл прокрутки (тройной массив)
- ✅ **Кнопки ← → для ручного управления**
- ✅ **Hover → увеличенный zoom preview**
- ✅ Пауза при наведении мыши
- ✅ Индикаторы-точки внизу
- ✅ Клик на варианте → мгновенный выбор (без кнопки "Выбрать")

#### `/components/EnvelopeCarousel.tsx`
```typescript
interface EnvelopeCarouselProps {
  options: EnvelopeOption[]
  selectedId: string | null
  onSelect: (id: string) => void
}
```

**Функции:**
- ✅ Показывает 3 варианта конверта одновременно
- ✅ Автоматическое переключение каждую **1 секунду**
- ✅ Плавная анимация сдвига влево (duration: 1000ms)
- ✅ Бесконечный цикл прокрутки (9 вариантов)
- ✅ **Кнопки ← → для ручного управления**
- ✅ **Hover → увеличенный zoom preview**
- ✅ Пауза при наведении мыши
- ✅ Индикаторы-точки внизу
- ✅ Клик на конверте → мгновенный выбор (без кнопки "Выбрать")

---

### 2. **Переработан `/components/PackagingModal.tsx`**

#### Структура модального окна:

```
┌─────────────────────────────────────────────┐
│  Добавьте к заказу подарочную упаковку      │
├─────────────────────────────────────────────┤
│                                             │
│  ○ Электронный сертификат                   │
│                                             │
│  ○ Электронный сертификат в фирменном       │
│     конверте                                │
│     └─► [EnvelopeCarousel]                  │
│                                             │
│  ○ Электронный сертификат в подарочной      │
│     упаковке и фирменном конверте           │
│     └─► [PackagingCarousel]                 │
│          └─► [EnvelopeCarousel]             │
│                                             │
└─────────────────────────────────────────────┘
```

#### Логика работы:

1. **При выборе "Электронный сертификат":**
   - Никаких дополнительных опций
   - Минималистичный выбор

2. **При выборе "В фирменном конверте":**
   - Появляется `EnvelopeCarousel` с 9 вариантами
   - Автоматическая анимация
   - Hover для zoom preview

3. **При выборе "В подарочной упаковке":**
   - Появляется `PackagingCarousel` с 4 вариантами
   - После выбора упаковки → появляется `EnvelopeCarousel`
   - Двухэтапный выбор: упаковка + конверт

---

### 3. **Варианты упаковок (4 штуки)**

```typescript
const packagingOptions = [
  { id: 'kraft', name: 'Крафтовая с апельсином и корицей', gradient: 'from-orange-100 to-orange-200' },
  { id: 'pink', name: 'Светло-розовая с цветами и узором', gradient: 'from-pink-100 to-pink-200' },
  { id: 'beige', name: 'Лиловая-бежевая с цветком и лентой', gradient: 'from-purple-100 to-pink-100' },
  { id: 'green', name: 'Светло-зелёная с жёлтоватой лентой', gradient: 'from-green-100 to-yellow-100' }
]
```

### 4. **Варианты конвертов (9 штук)**

```typescript
const envelopeOptions = [
  { id: 'white', name: 'Белый', color: '#e5e7eb', sealColor: '#d1d5db' },
  { id: 'emerald', name: 'Изумрудный', color: '#065f46', sealColor: '#fbbf24' },
  { id: 'pink', name: 'Нежно-розовый', color: '#f9a8d4', sealColor: '#8b5cf6' },
  { id: 'blue', name: 'Небесно-голубой', color: '#3b82f6', sealColor: '#f3f4f6' },
  { id: 'yellow', name: 'Желтый', color: '#fbbf24', sealColor: '#8b5cf6' },
  { id: 'dark', name: 'Брутальный черный', color: '#374151', sealColor: '#374151' },
  { id: 'red', name: 'Ярко-алый', color: '#ef4444', sealColor: '#dc2626' },
  { id: 'navy', name: 'Темно-синий', color: '#1e3a8a', sealColor: '#b45309' },
  { id: 'kraft', name: 'Лаконичный крафт', color: '#d2b48c', sealColor: '#dc2626' }
]
```

---

### 5. **Откат CheckoutModal.tsx**

✅ **Удалены:**
- Компоненты `PackagingCarousel` и `EnvelopeCarousel` из CheckoutModal
- State `isPaused` для каруселей
- Логика автоматической прокрутки

✅ **Восстановлены:**
- Статичные сетки: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
- Заголовки: "Выберите упаковку" и "Выберите конверт"
- Простой маппинг массивов без карусели

---

## 🎨 Визуальные особенности

### Hover Effect (Zoom Preview)

При наведении мыши на вариант упаковки/конверта:

```css
.hover → scale-110 z-20
+ Overlay с белым фоном
+ Увеличенное изображение
+ Название варианта
+ Fade-in анимация (200ms)
```

### Кнопки управления

```
┌────────────────────────────────────────┐
│                                        │
│  ←  [Item 1] [Item 2] [Item 3]  →     │
│                                        │
│           ● ━━━ ● ● ●                  │
└────────────────────────────────────────┘
```

- **← →** - Круглые кнопки с зелёной рамкой
- **●** - Индикаторы (активный растягивается)
- **Hover** - Светло-зелёный фон кнопок

---

## 🔧 Технические детали

### Бесшовная прокрутка

```typescript
// Создаём тройной массив
const extendedOptions = [...options, ...options, ...options]
const startIndex = options.length + currentIndex

// Transform с учётом gap
transform: `translateX(calc(-${startIndex * 33.333}% - ${startIndex * 0.75}rem))`
```

### Автоматическая анимация

```typescript
useEffect(() => {
  if (isPaused) return
  
  const interval = setInterval(() => {
    setCurrentIndex((prev) => (prev + 1) % options.length)
  }, 1000)
  
  return () => clearInterval(interval)
}, [options.length, isPaused])
```

### Пауза при hover

```typescript
<div 
  onMouseEnter={() => setIsPaused(true)}
  onMouseLeave={() => setIsPaused(false)}
>
  {/* Карусель */}
</div>
```

---

## 📂 Файловая структура

```
/components
├── PackagingCarousel.tsx       ← ✨ НОВЫЙ
├── EnvelopeCarousel.tsx        ← ✨ НОВЫЙ
├── PackagingModal.tsx          ← ✅ ОБНОВЛЁН
└── CheckoutModal.tsx           ← ✅ ОТКАЧЕН
```

---

## 🎯 Место применения

### `PackagingModal` вызывается в:

1. **`/components/GiftOptionsSection.tsx`** (Главная страница)
   - Кнопка: "🎁 Добавить подарочную упаковку"
   - Цвет: Зелёный градиент (emerald-teal)

2. **`/components/DeliveryOptionsSection.tsx`** (Страница "Доставка и оплата")
   - Кнопка: "Добавить упаковку" с иконкой Gift
   - Цвет: Жёлтый/Emerald градиент

### Карусели используются НАПРЯМУЮ (без модального окна):

3. **`/components/PetFriendlyGlampingPage.tsx`** ⭐ ОБНОВЛЕНО! (Страница "Сертификат для отдыха с питомцем")
   - Компонент: `PackagingCTASection`
   - ❌ Без кнопки и модального окна
   - ✅ Три блока с встроенными каруселями:
     - **Блок 1:** PackagingCarousel (4 варианта упаковки, 1190 ₽)
     - **Блок 2:** EnvelopeCarousel (9 цветов конверта, 290 ₽)
     - **Блок 3:** Сетка изображений сертификатов (3 дизайна, 0 ₽)
   - Каждый блок имеет свой state для выбора
   ```typescript
   <PackagingModal 
     isOpen={isPackagingModalOpen}
     onClose={() => setIsPackagingModalOpen(false)}
   />
   ```

2. **Кнопка "Добавить упаковку" (Gift иконка)**
   - Находится в секции доставки
   - Открывает модальное окно с выбором

---

## 🚀 Как это работает для пользователя

### Сценарий 1: Электронный сертификат
```
1. Открыть модальное окно
2. Выбрать "Электронный сертификат"
3. Закрыть окно
✅ Готово!
```

### Сценарий 2: Сертификат в конверте
```
1. Открыть модальное окно
2. Выбрать "В фирменном конверте"
3. Появляется карусель с конвертами
4. Карусель автоматически крутится
5. Навести мышь → пауза + zoom
6. Кликнуть на конверт → выбран
✅ Готово!
```

### Сценарий 3: Полный набор
```
1. Открыть модальное окно
2. Выбрать "В подарочной упаковке"
3. Появляется карусель с упаковками
4. Выбрать упаковку
5. Появляется карусель с конвертами
6. Выбрать конверт
✅ Готово!
```

---

## ✨ Ключевые улучшения

### До:
- ❌ Статичный список упаковок
- ❌ Нужна кнопка "Выбрать"
- ❌ Нет предпросмотра
- ❌ Скучная сетка

### После:
- ✅ Динамическая карусель с автоскроллом
- ✅ Мгновенный выбор по клику
- ✅ Zoom preview при hover
- ✅ Стрелки для ручного управления
- ✅ Индикаторы прогресса
- ✅ Плавные анимации
- ✅ Пауза при наведении

---

## 🧪 Тестирование

### Чек-лист:

- [x] Карусель автоматически переключается каждую секунду
- [x] При hover карусель останавливается
- [x] При hover показывается увеличенный preview
- [x] Кнопки ← → работают корректно
- [x] Индикаторы-точки синхронизированы с текущим слайдом
- [x] Клик на вариант сразу выбирает его
- [x] При выборе упаковки показывается карусель конвертов
- [x] CheckoutModal вернулся к статичным сеткам
- [x] Заголовки в CheckoutModal исправлены

---

## 📝 Замечания

### Почему нет синхронизации с CheckoutModal?

PackagingModal служит для **визуального превью** и выбора дизайна **перед добавлением в корзину**. 

Реальный выбор упаковки происходит в **CheckoutModal при оформлении заказа**, где пользователь может изменить свои предпочтения независимо от того, что было выбрано ранее.

---

## 🔮 Будущие улучшения

- [ ] Touch-свайпы для мобильных устройств
- [ ] Индикатор паузы (⏸️) при hover
- [ ] Адаптивная скорость в зависимости от количества элементов
- [ ] Сохранение выбора в localStorage
- [ ] Интеграция с корзиной (передача выбранной упаковки)
- [ ] Анимация появления конверта после выбора упаковки
- [ ] Предпросмотр итогового варианта (упаковка + конверт)

---

**Статус:** ✅ Завершено  
**Автор:** Figma Make AI  
**Дата:** 3 октября 2025