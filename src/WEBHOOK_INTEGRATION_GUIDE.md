# 🔗 Руководство по интеграции Webhook для корпоративных заявок

## 📋 Содержание
1. [Быстрый старт](#быстрый-старт)
2. [Формат данных](#формат-данных)
3. [Примеры данных](#примеры-данных)
4. [Настройка webhook URL](#настройка-webhook-url)
5. [Обработка на стороне сервера](#обработка-на-стороне-сервера)
6. [Тестирование](#тестирование)

---

## 🚀 Быстрый старт

### Где менять URL webhook?

Откройте файл `/config/webhooks.ts` и измените URL:

```typescript
export const CORPORATE_REQUEST_WEBHOOK_URL = 'https://your-webhook-url.com/api/corporate-requests';
```

**Вот и всё!** Теперь все заявки будут отправляться на ваш webhook.

---

## 📦 Формат данных

### Структура JSON

Каждая заявка отправляется POST-запросом в формате JSON со следующей структурой:

```typescript
interface CorporateRequestData {
  // ОБЯЗАТЕЛЬНЫЕ ПОЛЯ
  name: string;           // Имя контактного лица
  company: string;        // Название компании
  email: string;          // Email для связи
  phone: string;          // Телефон в формате +7 (xxx) xxx-xx-xx
  
  // НЕОБЯЗАТЕЛЬНЫЕ ПОЛЯ
  employees?: string;     // Количество сотрудников
  message?: string;       // Пожелания и детали заказа
  
  // МЕТАДАННЫЕ (автоматически добавляются)
  timestamp: string;      // Дата и время отправки (ISO 8601)
  source: string;         // Источник заявки
  userAgent?: string;     // User-agent браузера
  pageUrl?: string;       // URL страницы
}
```

---

## 📝 Примеры данных

### Пример 1: Полная заявка

```json
{
  "name": "Иван Петров",
  "company": "ООО Рога и Копыта",
  "email": "ivan.petrov@company.ru",
  "phone": "+7 (912) 345-67-89",
  "employees": "150",
  "message": "Интересуют сертификаты для команды на новогодние корпоративы. Нужно 150 штук с брендированием под наш логотип.",
  "timestamp": "2025-11-07T14:30:00.000Z",
  "source": "corporate_modal",
  "userAgent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
  "pageUrl": "https://глэмпинги.рф/corporate"
}
```

### Пример 2: Минимальная заявка

```json
{
  "name": "Мария Сидорова",
  "company": "ИП Сидоров",
  "email": "maria@example.com",
  "phone": "+7 (999) 888-77-66",
  "timestamp": "2025-11-07T15:45:00.000Z",
  "source": "corporate_modal",
  "userAgent": "Mozilla/5.0 (iPhone; CPU iPhone OS 15_0 like Mac OS X)",
  "pageUrl": "https://глэмпинги.рф/corporate"
}
```

### Пример 3: Заявка с подробным описанием

```json
{
  "name": "Алексей Смирнов",
  "company": "АО Северсталь",
  "email": "a.smirnov@severstal.com",
  "phone": "+7 (921) 111-22-33",
  "employees": "500",
  "message": "Рассматриваем варианты корпоративных подарков для 500 сотрудников на 23 февраля и 8 марта. Интересует:\n1. Сертификаты номиналом 25 000₽\n2. Брендирование с логотипом компании\n3. Доставка в офисы в Москве и Санкт-Петербурге\n4. Возможность отсрочки платежа",
  "timestamp": "2025-11-07T16:20:00.000Z",
  "source": "corporate_modal",
  "userAgent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7)",
  "pageUrl": "https://глэмпинги.рф/corporate"
}
```

---

## ⚙️ Настройка webhook URL

### Вариант 1: Простая настройка (рекомендуется)

Просто измените URL в файле `/config/webhooks.ts`:

```typescript
export const CORPORATE_REQUEST_WEBHOOK_URL = 'https://ваш-сервер.com/api/webhook';
```

### Вариант 2: Настройка для разных окружений

Если вам нужны разные URL для разработки, тестирования и продакшена:

```typescript
export const WEBHOOK_URLS = {
  production: 'https://api.производство.com/webhook',
  staging: 'https://api.тест.com/webhook',
  development: 'http://localhost:3000/webhook',
};

// Измените это значение в зависимости от окружения
const ENVIRONMENT: 'production' | 'staging' | 'development' = 'production';
```

---

## 🖥️ Обработка на стороне сервера

### Пример на Node.js (Express)

```javascript
const express = require('express');
const app = express();

app.use(express.json());

app.post('/api/corporate-requests', async (req, res) => {
  try {
    const requestData = req.body;
    
    // Валидация обязательных полей
    if (!requestData.name || !requestData.company || 
        !requestData.email || !requestData.phone) {
      return res.status(400).json({ 
        error: 'Отсутствуют обязательные поля' 
      });
    }
    
    // Здесь ваша логика:
    // - Сохранение в базу данных
    // - Отправка email уведомлений
    // - Интеграция с CRM
    // - и т.д.
    
    console.log('Новая корпоративная заявка:', requestData);
    
    // Ответ клиенту
    res.status(200).json({ 
      success: true,
      message: 'Заявка успешно получена',
      requestId: Date.now() // Или ID из вашей БД
    });
    
  } catch (error) {
    console.error('Ошибка обработки заявки:', error);
    res.status(500).json({ 
      error: 'Ошибка сервера' 
    });
  }
});

app.listen(3000, () => {
  console.log('Webhook сервер запущен на порту 3000');
});
```

### Пример на Python (Flask)

```python
from flask import Flask, request, jsonify
from datetime import datetime

app = Flask(__name__)

@app.route('/api/corporate-requests', methods=['POST'])
def handle_corporate_request():
    try:
        data = request.get_json()
        
        # Валидация
        required_fields = ['name', 'company', 'email', 'phone']
        if not all(field in data for field in required_fields):
            return jsonify({'error': 'Отсутствуют обязательные поля'}), 400
        
        # Ваша логика обработки
        print(f"Новая заявка от {data['company']}: {data['name']}")
        
        # Здесь можно:
        # - Сохранить в БД
        # - Отправить email
        # - Создать задачу в CRM
        
        return jsonify({
            'success': True,
            'message': 'Заявка получена',
            'timestamp': datetime.now().isoformat()
        }), 200
        
    except Exception as e:
        print(f"Ошибка: {e}")
        return jsonify({'error': 'Ошибка сервера'}), 500

if __name__ == '__main__':
    app.run(port=3000)
```

---

## 🧪 Тестирование

### 1. Локальное тестирование с ngrok

```bash
# Установите ngrok (если еще не установлен)
brew install ngrok  # для macOS
# или скачайте с https://ngrok.com

# Запустите ваш локальный сервер на порту 3000
node server.js

# В другом терминале запустите ngrok
ngrok http 3000

# Скопируйте URL из вывода (например, https://abc123.ngrok.io)
# и вставьте его в /config/webhooks.ts:
export const CORPORATE_REQUEST_WEBHOOK_URL = 'https://abc123.ngrok.io/api/corporate-requests';
```

### 2. Тестирование с помощью curl

```bash
curl -X POST https://your-webhook-url.com/api/corporate-requests \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест",
    "company": "Test Company",
    "email": "test@example.com",
    "phone": "+7 (999) 999-99-99",
    "employees": "100",
    "message": "Тестовое сообщение",
    "timestamp": "2025-11-07T12:00:00.000Z",
    "source": "test"
  }'
```

### 3. Сервисы для тестирования webhook

- **webhook.site** - https://webhook.site
  - Моментально получаете тестовый URL
  - Видите все входящие запросы в реальном времени
  - Не требует регистрации

- **RequestBin** - https://requestbin.com
  - Аналогично webhook.site
  - Удобный интерфейс для просмотра запросов

**Как использовать:**

1. Откройте https://webhook.site
2. Скопируйте "Your unique URL"
3. Вставьте в `/config/webhooks.ts`
4. Отправьте тестовую заявку через форму на сайте
5. Обновите страницу webhook.site - увидите данные

---

## 📊 Метаданные запроса

Каждая заявка автоматически обогащается метаданными:

| Поле | Тип | Описание | Пример |
|------|-----|----------|--------|
| `timestamp` | string | Дата/время отправки в формате ISO 8601 | `"2025-11-07T14:30:00.000Z"` |
| `source` | string | Источник заявки | `"corporate_modal"` |
| `userAgent` | string | Информация о браузере пользователя | `"Mozilla/5.0..."` |
| `pageUrl` | string | URL страницы, откуда отправлена заявка | `"https://глэмпинги.рф/corporate"` |

### Возможные значения `source`:

- `"corporate_modal"` - Модальная форма на странице корпоративных подарков
- `"corporate_b2b_page"` - Форма на странице B2B (если добавлена)
- `"test"` - Тестовая заявка

---

## 🔐 Безопасность

### Рекомендации:

1. **Используйте HTTPS** для webhook URL
2. **Валидируйте данные** на стороне сервера
3. **Добавьте rate limiting** для защиты от спама
4. **Используйте токены** для аутентификации (опционально)

### Пример с токеном (опционально):

Можно добавить секретный токен в headers:

```typescript
// В /utils/webhookService.ts
const response = await fetch(CORPORATE_REQUEST_WEBHOOK_URL, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-Webhook-Secret': 'ваш-секретный-токен-здесь' // Добавьте эту строку
  },
  body: JSON.stringify(requestData),
});
```

---

## ❓ FAQ

**Q: Что делать, если webhook не работает?**

A: Проверьте:
1. Правильность URL в `/config/webhooks.ts`
2. Доступность вашего сервера (он должен быть онлайн)
3. CORS настройки на сервере
4. Логи в консоли браузера (F12 → Console)

**Q: Как узнать, что заявка успешно отправлена?**

A: Смотрите консоль браузера. Успешная отправка: `✅ Заявка успешно отправлена на webhook`

**Q: Можно ли отправлять данные в несколько мест одновременно?**

A: Да! Просто вызовите `sendCorporateRequest` несколько раз с разными URL или модифицируйте функцию для работы с массивом URL.

**Q: Что происходит при ошибке отправки?**

A: Пользователь все равно видит экран успеха (для UX), но в консоль выводится ошибка. Вы можете изменить это поведение в `/components/CorporateRequestFormModal.tsx`.

---

## 📞 Поддержка

Если возникли вопросы по интеграции webhook, проверьте:
- Логи в консоли браузера (F12)
- Файл `/utils/webhookService.ts` - там вся логика отправки
- Файл `/config/webhooks.ts` - там настройки URL

---

**Готово! Теперь все корпоративные заявки автоматически отправляются на ваш webhook! 🎉**
