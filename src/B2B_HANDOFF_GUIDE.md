# 🎯 B2B Well-being Решение - Handoff для разработчика

## 📦 Что передаем:
Две высококонверсионные B2B страницы с Well-being фокусом

---

## 📁 СПИСОК ФАЙЛОВ ДЛЯ ПЕРЕДАЧИ

### 🎨 **Основные страницы** (2 файла)
```
components/
├── CorporateB2BPage.tsx          ← v1: Well-being фокус
└── CorporateB2BPageV2.tsx        ← v2: ROI + Аналитика фокус
```

### 🧩 **Зависимые компоненты** (5 файлов)
```
components/
├── CorporateQuizSection.tsx      ← Интерактивный квиз (4 шага)
├── CorporateGiftReasonsSection.tsx ← Блок "Почему выбирают"
├── TypewriterText.tsx            ← Анимация печатающегося текста
├── FoxIllustrations.tsx          ← SVG иллюстрации лисенка
├── Header.tsx                    ← Шапка сайта
└── Footer.tsx                    ← Футер сайта
```

### 🎨 **UI компоненты Shadcn** (вся папка ui/)
```
components/ui/
├── button.tsx
├── input.tsx
├── checkbox.tsx
├── progress.tsx
├── card.tsx
├── badge.tsx
└── ... (все остальные из папки ui/)
```

### 🖼️ **Утилиты для изображений**
```
components/figma/
└── ImageWithFallback.tsx         ← Компонент для картинок с fallback
```

### 🎨 **Стили**
```
styles/
└── globals.css                   ← Все токены и стили
```

---

## 📦 ИТОГО: Минимальный набор файлов

### **Вариант 1: Самостоятельные страницы (БЕЗ Header/Footer)**
Если разработчик интегрирует в существующий сайт:

```
📁 b2b-handoff/
  ├── components/
  │   ├── CorporateB2BPage.tsx
  │   ├── CorporateB2BPageV2.tsx
  │   ├── CorporateQuizSection.tsx
  │   ├── CorporateGiftReasonsSection.tsx
  │   ├── TypewriterText.tsx
  │   ├── FoxIllustrations.tsx
  │   ├── figma/
  │   │   └── ImageWithFallback.tsx
  │   └── ui/
  │       └── (вся папка, ~40 файлов)
  ├── styles/
  │   └── globals.css
  ├── INTEGRATION.md (инструкция ниже)
  └── package.json (зависимости ниже)
```

**Итого: ~48 файлов**

---

### **Вариант 2: Полные страницы (С Header/Footer)**
Если разработчик создает отдельный раздел:

```
📁 b2b-handoff/
  ├── components/
  │   ├── CorporateB2BPage.tsx
  │   ├── CorporateB2BPageV2.tsx
  │   ├── CorporateQuizSection.tsx
  │   ├── CorporateGiftReasonsSection.tsx
  │   ├── TypewriterText.tsx
  │   ├── FoxIllustrations.tsx
  │   ├── Header.tsx               ← +
  │   ├── Footer.tsx               ← +
  │   ├── figma/
  │   │   └── ImageWithFallback.tsx
  │   └── ui/
  │       └── (вся папка)
  ├── styles/
  │   └── globals.css
  ├── INTEGRATION.md
  └── package.json
```

**Итого: ~50 файлов**

---

## 🔧 NPM Зависимости

Создай файл **package.json** с зависимостями:

```json
{
  "name": "b2b-wellbeing-pages",
  "version": "1.0.0",
  "description": "B2B Well-being страницы для глэмпинг сертификатов",
  "dependencies": {
    "react": "^18.0.0",
    "lucide-react": "^0.263.1",
    "motion": "^10.16.2",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
```

### **Команда установки:**
```bash
npm install lucide-react motion
```

---

## 🚀 INTEGRATION.md - Инструкция для разработчика

```markdown
# Интеграция B2B Well-being страниц

## 1️⃣ Установка зависимостей

npm install lucide-react motion

## 2️⃣ Копирование файлов

1. Скопируй папку `components/` в свой проект
2. Скопируй `styles/globals.css` (или импортируй токены)
3. Убедись, что Tailwind CSS v4 настроен

## 3️⃣ Настройка роутинга

### Next.js App Router:
// app/corporate-b2b/page.tsx
import { CorporateB2BPage } from '@/components/CorporateB2BPage'

export default function Page() {
  return <CorporateB2BPage 
    onNavigateToHome={() => router.push('/')}
    onNavigateToGiftOptions={() => router.push('/gift-options')}
    // ... остальные пропсы
  />
}

### React Router:
import { CorporateB2BPage } from './components/CorporateB2BPage'

<Route path="/corporate-b2b" element={
  <CorporateB2BPage 
    onNavigateToHome={() => navigate('/')}
    onNavigateToGiftOptions={() => navigate('/gift-options')}
    // ...
  />
} />

## 4️⃣ Варианты интеграции

### Вариант А: БЕЗ Header/Footer
Если у вас уже есть Header/Footer:

1. Удали импорты Header и Footer из CorporateB2BPage.tsx
2. Оберни страницу своими компонентами:
   <YourHeader />
   <CorporateB2BPage {...props} />
   <YourFooter />

### Вариант Б: С Header/Footer
Используй страницы как есть - они содержат полный layout

## 5️⃣ Подключение квиза к API

В файле `CorporateQuizSection.tsx` найди функцию `handleSubmit`:

const handleSubmit = () => {
  console.log('Quiz completed:', quizData) // ← ЗАМЕНИ НА API CALL
  
  // Пример интеграции:
  fetch('/api/corporate-quiz', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(quizData)
  })
}

## 6️⃣ Настройка изображений

Компонент `ImageWithFallback` используется для всех картинок.
Замени URL изображений на свои в компонентах.

## 7️⃣ Цветовая схема

Страницы используют токены из `globals.css`:
- emerald-400/500/600 (зеленый)
- teal-500/600 (бирюзовый)
- cyan-500 (голубой)

Если нужно изменить цвета - редактируй токены в globals.css

## 8️⃣ Адаптивность

✅ Полностью адаптивно (mobile-first)
✅ Breakpoints: sm (640px), md (768px), lg (1024px), xl (1280px)
✅ Протестировано на iPhone, iPad, Desktop

## 9️⃣ Accessibility

✅ Semantic HTML
✅ ARIA labels
✅ Keyboard navigation
✅ Screen reader friendly

## 🔟 Production Checklist

- [ ] Установлены npm зависимости
- [ ] Скопированы все файлы
- [ ] Настроен роутинг
- [ ] Подключен API endpoint для квиза
- [ ] Заменены placeholder изображения
- [ ] Протестирована адаптивность
- [ ] Проверена производительность
- [ ] Настроен мониторинг отправки форм
```

---

## 🎯 Что делают страницы?

### **CorporateB2BPage (v1)** - Well-being фокус
- ✅ Hero с TypewriterText анимацией
- ✅ Научные факты (38% ↓ стресс, 52% ↑ лояльность)
- ✅ HR процесс (проблема → Well-being → результат)
- ✅ B2B пакеты (Старт, Профи, Премиум)
- ✅ Доверие компаний (Сбер, Яндекс, VK)
- ✅ Интерактивный квиз (4 шага)
- ✅ Блок причин выбора

### **CorporateB2BPageV2 (v2)** - ROI фокус
- ✅ Hero с акцентом на ROI
- ✅ ROI калькулятор (7.2× возврат инвестиций)
- ✅ Метрики до/после (↓ 45% текучесть, ↑ 32% производительность)
- ✅ Кейс-стади с реальными цифрами
- ✅ B2B пакеты с ROI
- ✅ Интерактивный квиз (4 шага)
- ✅ Блок причин выбора

---

## 📊 Технические детали

### **Анимации:**
- Motion (Framer Motion) для плавных переходов
- TypewriterText для эффекта печати
- Hover эффекты на карточках

### **Формы:**
- Валидация полей
- Автоформатирование телефона
- Сохранение в localStorage
- Progress bar квиза

### **Производительность:**
- Ленивая загрузка изображений (ImageWithFallback)
- Оптимизированные SVG иллюстрации
- Минимальные зависимости

---

## 🎨 Структура компонентов

```
CorporateB2BPage
├── Header
├── Hero Section
│   └── TypewriterText
├── Science Facts Section
├── HR Process Section
├── B2B Packages Section
├── Trust Section
├── CorporateGiftReasonsSection
├── CorporateQuizSection
│   ├── Step 1: Количество сотрудников
│   ├── Step 2: Бюджет
│   ├── Step 3: Формат вручения
│   └── Step 4: Контакты
└── Footer
```

---

## 📞 Контакты и поддержка

Если у разработчика возникнут вопросы:
- Все компоненты документированы
- TypeScript типизация
- Понятная структура файлов

---

## ✅ Финальный чеклист передачи

### Файлы:
- [ ] CorporateB2BPage.tsx
- [ ] CorporateB2BPageV2.tsx
- [ ] CorporateQuizSection.tsx
- [ ] CorporateGiftReasonsSection.tsx
- [ ] TypewriterText.tsx
- [ ] FoxIllustrations.tsx
- [ ] ImageWithFallback.tsx
- [ ] Header.tsx (опционально)
- [ ] Footer.tsx (опционально)
- [ ] /ui/* (вся папка)
- [ ] globals.css

### Документация:
- [ ] INTEGRATION.md
- [ ] package.json с зависимостями
- [ ] README с описанием страниц

### Тестирование:
- [ ] Проверены все ссылки
- [ ] Протестированы формы
- [ ] Проверена адаптивность
- [ ] Проверены анимации

---

## 🚀 Быстрый старт для разработчика

1. Распакуй архив `b2b-handoff.zip`
2. `npm install`
3. Скопируй файлы в проект
4. Добавь роуты `/corporate-b2b` и `/corporate-b2b-v2`
5. Подключи API для квиза
6. Готово! 🎉

---

**Версия:** 1.0  
**Дата:** 27 октября 2025  
**Дизайн:** Cuva-inspired, emerald/teal цветовая схема  
**Фреймворк:** React + Tailwind CSS v4  
