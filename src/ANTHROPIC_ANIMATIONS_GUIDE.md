# 🎬 Anthropic AI Animated Delivery Section - Animation Guide

## Обзор

Создана полностью анимированная версия секции выбора способа доставки с использованием Motion (Framer Motion) и эстетики Anthropic AI.

## 📁 Файлы

- **Анимированный компонент**: `/components/AnthropicAnimatedDeliverySection.tsx`
- **Базовый компонент**: `/components/AnthropicGiftDeliverySection.tsx`
- **Тестовая страница**: `/components/AnthropicDeliveryTestPage.tsx` (с переключателем версий)

## 🚀 Как протестировать

1. Откройте главную страницу
2. Нажмите **оранжевую кнопку Dev Navigation** (справа внизу)
3. Выберите **"Тест Anthropic AI доставки"** (желтая кнопка)
4. Используйте переключатель **"Базовый"** / **"С анимациями"** в шапке

## 🎨 Анимации в деталях

### 1. Header Animation (Заголовок)

#### Иконка Gift
```tsx
initial: { scale: 0, rotate: -180 }
animate: { scale: 1, rotate: 0 }
duration: 0.6s
delay: 0.2s
easing: ease-out
```
**Эффект**: Иконка "вращается" и масштабируется от 0 до 1

#### Заголовок "Дарите как хотите!"
```tsx
initial: { opacity: 0, y: 20 }
animate: { opacity: 1, y: 0 }
duration: 0.6s
delay: 0.3s
easing: ease-out
```
**Эффект**: Плавное появление снизу вверх

#### Подзаголовок
```tsx
initial: { opacity: 0, y: 20 }
animate: { opacity: 1, y: 0 }
duration: 0.6s
delay: 0.4s
easing: ease-out
```
**Эффект**: Появляется после заголовка с задержкой

---

### 2. Delivery Cards - Stagger Animation (Карточки доставки)

Каждая карточка появляется с задержкой 0.1s после предыдущей:

```tsx
Card 1 (Electronic): delay = 0.5s
Card 2 (Envelope):   delay = 0.6s
Card 3 (Premium):    delay = 0.7s
Card 4 (Custom):     delay = 0.8s
```

#### Анимация появления карточки
```tsx
initial: { opacity: 0, y: 30 }
animate: { opacity: 1, y: 0 }
duration: 0.6s
easing: ease-out
```

#### Hover состояние
```tsx
whileHover: { 
  y: -4,
  boxShadow: '0 8px 24px rgba(204, 120, 92, 0.15)'
}
duration: 0.3s
easing: ease-out
```
**Эффект**: Карточка поднимается на 4px с тенью

#### Tap (клик) состояние
```tsx
whileTap: { scale: 0.98 }
```
**Эффект**: Небольшое сжатие при клике

---

### 3. Badge Animation (Бейджи "Рекомендуем" / "Премиум")

```tsx
initial: { opacity: 0, scale: 0.8 }
animate: { opacity: 1, scale: 1 }
duration: 0.4s
delay: 0.7s + (index * 0.1)
easing: ease-out
```
**Эффект**: Бейдж появляется с масштабированием после карточки

---

### 4. Checkmark Animation (Галочка выбора) ✅

#### Появление
```tsx
initial: { scale: 0, rotate: -180 }
animate: { scale: 1, rotate: 0 }
duration: 0.4s
easing: ease-out
```

#### Исчезновение
```tsx
exit: { scale: 0, rotate: 180 }
duration: 0.4s
easing: ease-out
```

**Эффект**: Галочка **вращается на 180°** при появлении и исчезновении - это создает эффект "закручивания"

---

### 5. Icon Hover Animation (Иконки в карточках)

```tsx
animate: { 
  scale: isHovered ? 1.1 : 1,
  backgroundColor: [меняется в зависимости от состояния]
}
duration: 0.3s
easing: ease-out
```

**Эффект**: Иконка увеличивается на 10% и меняет цвет фона при hover

---

### 6. Carousel Animation (Карусель сертификатов)

#### Slide Variants (Направления)
```tsx
enter: (direction) => ({
  x: direction > 0 ? 1000 : -1000,
  opacity: 0
})

center: {
  x: 0,
  opacity: 1
}

exit: (direction) => ({
  x: direction < 0 ? 1000 : -1000,
  opacity: 0
})
```

#### Transition настройки
```tsx
x: { 
  type: "spring", 
  stiffness: 300, 
  damping: 30 
}
opacity: { 
  duration: 0.3 
}
```

**Эффект**: Плавное скольжение слайдов с spring-эффектом (пружинистое движение)

---

### 7. Navigation Arrows Animation (Стрелки карусели)

```tsx
whileHover: { 
  scale: 1.1,
  boxShadow: '0 4px 12px rgba(92, 74, 58, 0.15)'
}

whileTap: { 
  scale: 0.95 
}

duration: 0.2s
easing: ease-out
```

**Эффект**: Стрелки увеличиваются при hover и сжимаются при клике

---

### 8. Navigation Dots Animation (Точки навигации)

#### Активная точка
```tsx
animate: { 
  width: currentSlide === index ? '32px' : '8px'
}
duration: 0.3s
easing: ease-out
```

#### Hover
```tsx
whileHover: { scale: 1.2 }
```

**Эффект**: Активная точка **расширяется** до 32px, остальные 8px. При hover любая точка увеличивается на 20%

---

### 9. CTA Button Animation (Кнопка призыва к действию)

```tsx
whileHover: { 
  y: -2,
  backgroundColor: '#4a3a2e',
  boxShadow: '0 6px 16px rgba(92, 74, 58, 0.3)'
}

whileTap: { 
  scale: 0.98 
}

duration: 0.2s
easing: ease-out
```

**Эффект**: Кнопка поднимается на 2px, темнеет и получает больше тени

#### Текст под кнопкой
```tsx
initial: { opacity: 0 }
animate: { opacity: 0.7 }
duration: 0.6s
delay: 1.3s
easing: ease-out
```

---

## 📊 Timing Timeline (Хронология появления)

```
0.0s  - Начало
0.2s  - Иконка Gift появляется
0.3s  - Заголовок появляется
0.4s  - Подзаголовок появляется
0.5s  - Card 1 появляется
0.6s  - Card 2 появляется
0.7s  - Card 3 появляется + Badge Card 1
0.8s  - Card 4 появляется + Badge Card 2
0.9s  - Carousel секция появляется + Badge Card 3
1.1s  - CTA кнопка появляется
1.3s  - Текст под кнопкой появляется
```

**Общая длительность загрузки анимаций**: ~1.3 секунды

---

## 🎭 Интерактивные состояния

### Default (По умолчанию)
- Все элементы на месте после завершения входных анимаций
- Электронный сертификат имеет primary стиль

### Hover (Наведение на карточку)
- ✅ Карточка поднимается на 4px
- ✅ Появляется тень
- ✅ Рамка меняется на оранжевую
- ✅ Иконка масштабируется 1.1x
- ✅ Фон иконки светлеет

### Click/Tap (Клик на карточку)
- ✅ Карточка сжимается до 0.98x (feedback)
- ✅ Галочка вращается и появляется (0.4s)
- ✅ Рамка становится 3px оранжевой
- ✅ Фон меняется на светло-оранжевый

### Carousel Navigation
- ✅ Слайды скользят с spring эффектом
- ✅ Стрелки увеличиваются при hover
- ✅ Точки расширяются (активная) или увеличиваются (hover)

---

## 🔧 Технические детали

### Библиотека анимаций
```tsx
import { motion, AnimatePresence } from 'motion/react'
```

**Motion (ранее Framer Motion)** - мощная библиотека для декларативных анимаций в React

### Key Props

#### `motion.div` / `motion.button`
- Обычные HTML элементы с суперсилами анимации

#### `initial`
- Начальное состояние перед появлением

#### `animate`
- Целевое состояние анимации

#### `exit`
- Состояние при удалении (требует `AnimatePresence`)

#### `whileHover`
- Состояние при наведении курсора

#### `whileTap`
- Состояние при клике/касании

#### `transition`
- Настройки анимации (duration, delay, easing, type)

### AnimatePresence
```tsx
<AnimatePresence initial={false} custom={direction} mode="wait">
  {/* Компоненты с exit анимацией */}
</AnimatePresence>
```

Позволяет анимировать компоненты при их удалении из DOM

---

## 🎨 Easing Functions

Все анимации используют **ease-out** для естественного замедления:

```tsx
transition: { duration: 0.6, ease: 'easeOut' }
```

### Исключения:

**Spring для карусели** (более реалистичное движение):
```tsx
x: { type: "spring", stiffness: 300, damping: 30 }
```

---

## ⚡ Performance оптимизация

### ✅ Что сделано:

1. **GPU ускорение**: Используются `transform` свойства (x, y, scale, rotate)
2. **Избегание layout shifts**: Не анимируем width/height напрямую (только через transform)
3. **AnimatePresence**: Правильное удаление элементов из DOM
4. **Spring physics**: Для карусели используется физическая модель вместо CSS transitions

### ❌ Чего избегаем:

- ❌ Анимация left/top (используем x/y)
- ❌ Анимация width/height без transform
- ❌ Слишком длинные задержки (макс 1.3s)
- ❌ Многочисленные одновременные анимации

---

## 🎯 Кастомизация анимаций

### Изменить скорость появления карточек
```tsx
delay: 0.5 + (index * 0.1) // Уменьшите 0.1 для быстрее, увеличьте для медленнее
```

### Изменить высоту подъема при hover
```tsx
whileHover: { y: -4 } // Измените -4 на другое значение
```

### Изменить угол вращения галочки
```tsx
initial: { scale: 0, rotate: -180 } // Измените -180 на другой угол
```

### Изменить spring настройки карусели
```tsx
x: { 
  type: "spring", 
  stiffness: 300,  // Жесткость (выше = быстрее)
  damping: 30      // Демпфирование (выше = меньше колебаний)
}
```

### Добавить новую анимацию
```tsx
<motion.div
  initial={{ opacity: 0, scale: 0.5 }}
  animate={{ opacity: 1, scale: 1 }}
  transition={{ duration: 0.5, ease: 'easeOut' }}
>
  Ваш контент
</motion.div>
```

---

## 🔄 Сравнение версий

### Базовая версия (AnthropicGiftDeliverySection)
- ✅ Статичные карточки с CSS transitions
- ✅ Простая карусель с transform
- ✅ Меньший размер бандла
- ❌ Нет stagger эффектов
- ❌ Нет сложных анимаций

### Анимированная версия (AnthropicAnimatedDeliverySection)
- ✅ Полная Motion анимация
- ✅ Stagger эффекты появления
- ✅ Вращающаяся галочка
- ✅ Spring физика для карусели
- ✅ Расширяющиеся точки навигации
- ❌ Больший размер бандла (+~50KB)

---

## 📱 Адаптивность анимаций

Анимации работают одинаково на всех устройствах, но учитывайте:

### Mobile
- ✅ Анимации работают плавно
- ⚠️ `whileHover` не работает на touch устройствах (это нормально)
- ✅ `whileTap` работает отлично

### Tablet
- ✅ Все анимации работают
- ✅ 2x2 grid сохраняет stagger

### Desktop
- ✅ Полный функционал
- ✅ Hover эффекты работают
- ✅ 4 колонки показывают красивый stagger

---

## 🐛 Troubleshooting

### Анимации не работают
1. Проверьте импорт: `import { motion } from 'motion/react'`
2. Убедитесь что элемент обернут в `motion.div`, а не просто `div`
3. Проверьте что `AnimatePresence` используется для exit анимаций

### Анимации дергаются
1. Проверьте что используете `transform` свойства (x, y, scale)
2. Избегайте анимации width/height
3. Уменьшите stiffness в spring настройках

### Carousel не переключается плавно
1. Убедитесь что `mode="wait"` установлен в `AnimatePresence`
2. Проверьте что `direction` правильно обновляется
3. Убедитесь что `key={currentSlide}` уникален

---

## 🎓 Дополнительные ресурсы

### Motion Documentation
- [Official Motion docs](https://motion.dev/)
- [Motion API Reference](https://motion.dev/docs/react-api)

### Animation Principles
- **Easing**: ease-out для естественного замедления
- **Duration**: 0.2-0.6s для UI элементов
- **Stagger**: 0.1s задержка между элементами
- **Spring**: Для реалистичных физических движений

---

## 💡 Best Practices

### ✅ Делать:
- Использовать `transform` для анимаций позиции
- Добавлять небольшие задержки (stagger)
- Использовать ease-out для UI элементов
- Тестировать на мобильных устройствах
- Использовать `AnimatePresence` для exit анимаций

### ❌ Не делать:
- Анимировать слишком много элементов одновременно
- Делать анимации длиннее 1 секунды (кроме особых случаев)
- Использовать сложные easing функции без необходимости
- Забывать про `key` prop в списках
- Анимировать layout свойства (width, height, padding)

---

## 🎉 Заключение

Анимированная версия добавляет профессиональную полировку и улучшает пользовательский опыт. Используйте переключатель на тестовой странице, чтобы сравнить обе версии и выбрать подходящую для вашего проекта!

**Рекомендация**: Используйте анимированную версию для production, если размер бандла не критичен. Визуальный эффект стоит дополнительных ~50KB.
