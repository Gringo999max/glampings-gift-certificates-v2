# Mobile Horizontal Scroll Update

## 📱 Обновление: Горизонтальная прокрутка на мобильных устройствах

### Дата: 5 ноября 2025

---

## 🎯 Выполненные задачи

### 1. ✅ Тестовая страница "Подарок, который оценят" (3 варианта)

**Файл:** `/components/CorporateGiftReasonsTestPage.tsx`

**Описание:**
Создана интерактивная тестовая страница для сравнения трех вариантов блока "Подарок, который оценят и запомнят надолго".

**Особенности:**
- 📊 Переключение между 4 вариантами (Оригинал + 3 новых)
- 🎨 Sticky header с описанием каждого варианта
- 💡 Информационный баннер с контекстом
- 📍 Bottom bar с индикатором текущего варианта

**Как открыть:**
1. Перейдите на главную страницу
2. Нажмите на оранжевую кнопку "Тестовые страницы" в правом нижнем углу
3. Выберите "🎁 Подарок который оценят (3 варианта)"

**Или напрямую:**
- URL route: `/corporate-gift-reasons-test`

---

### 2. ✅ Горизонтальная прокрутка для "Инвестируйте в каждого сотрудника"

**Файл:** `/components/B2BPackagesByPriceV2Variants.tsx`

**Компонент:** `Variant2` (По эмоциональному результату)

**Изменения:**
```tsx
// До:
<div className="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-6xl mx-auto">

// После:
<div className="flex md:grid md:grid-cols-3 gap-6 max-w-6xl mx-auto 
     overflow-x-auto md:overflow-visible snap-x snap-mandatory 
     scrollbar-hide pb-4 px-4 md:px-0">
```

**Карточки:**
```tsx
// Добавлены классы:
flex-shrink-0 w-[85vw] md:w-auto snap-center
```

**Функционал:**
- 📱 **Mobile:** Горизонтальная прокрутка с snap
- 🖥️ **Desktop:** Grid из 3 колонок (как было)
- 🎯 **Auto-scroll:** При загрузке на мобильных автоматически центрируется на среднем пакете (20 000 ₽)

**Технические детали:**
```javascript
const scrollContainerRef = React.useRef<HTMLDivElement>(null)

React.useEffect(() => {
  if (scrollContainerRef.current && window.innerWidth < 768) {
    const container = scrollContainerRef.current
    const cardWidth = container.scrollWidth / packages.length
    const centerPosition = cardWidth * 1 - (container.clientWidth / 2) + (cardWidth / 2)
    container.scrollTo({ left: centerPosition, behavior: 'smooth' })
  }
}, [])
```

---

### 3. ✅ Горизонтальная прокрутка для "Варианты оформления подарочного сертификата"

**Файлы:** 
- `/components/CorporateB2BPage.tsx` (PackagingCTASection)
- `/components/HowItWorksPage.tsx` (PackagingCTASection)

**Изменения:**
```tsx
// До:
<div className="flex flex-col lg:flex-row gap-6">

// После:
<div className="flex gap-6 overflow-x-auto lg:overflow-visible 
     snap-x snap-mandatory scrollbar-hide pb-4 px-4 lg:px-0">
```

**Карточки (все 3):**
```tsx
// Добавлены классы:
flex-shrink-0 w-[85vw] lg:w-auto snap-center
```

**Затронутые карточки:**
1. 🎁 Коробка ручной работы (border-purple-100)
2. ✉️ Фирменный конверт (border-amber-100)
3. 📱 Электронный сертификат (border-emerald-100)

---

### 4. ✅ Стили для скрытия scrollbar

**Файл:** `/styles/globals.css`

**Добавлено:**
```css
/* Hide scrollbar for horizontal scroll */
.scrollbar-hide {
  -ms-overflow-style: none;  /* IE and Edge */
  scrollbar-width: none;  /* Firefox */
}

.scrollbar-hide::-webkit-scrollbar {
  display: none;  /* Safari and Chrome */
}
```

**Поддержка браузеров:**
- ✅ Chrome/Safari (WebKit)
- ✅ Firefox
- ✅ Edge/IE

---

## 📐 CSS Классы для горизонтальной прокрутки

### Контейнер:
```css
flex                      /* Flexbox layout */
overflow-x-auto          /* Горизонтальный скролл на мобильных */
md:overflow-visible      /* Убрать скролл на desktop */
snap-x snap-mandatory    /* Snap scrolling */
scrollbar-hide          /* Скрыть scrollbar */
pb-4                    /* Padding bottom для тени */
px-4 md:px-0           /* Padding horizontal на мобильных */
```

### Карточки:
```css
flex-shrink-0           /* Не сжимать */
w-[85vw] или w-[92vw]  /* 85% или 92% ширины viewport на мобильных */
lg:w-auto              /* Auto width на desktop */
snap-center            /* Центрировать при snap */
```

**Обновление от 5 ноября:**
- Блок "Варианты оформления" теперь использует `w-[92vw]` для почти полноэкранных карточек
- Блок "Подарок, который оценят" использует `w-[85vw]`

---

## 🎨 UX Особенности

### Mobile (< 768px):
- 📱 Горизонтальная прокрутка свайпом
- 🎯 Snap к центру карточки
- 👆 Скрыт scrollbar для чистого вида
- 📏 Карточки занимают 85% ширины экрана
- 🔄 Smooth scroll при инициализации (пакеты)

### Desktop (≥ 768px):
- 🖥️ Grid/Flex layout
- 👁️ Все карточки видны сразу
- ✨ Hover эффекты работают
- 🎨 Scale animation на hover

---

## 🧪 Тестирование

### Проверьте на мобильных:
1. ✅ Свайп влево-вправо работает плавно
2. ✅ Snap центрирует карточки
3. ✅ Scrollbar скрыт
4. ✅ Padding корректный (нет обрезки теней)
5. ✅ Пакеты: автоматически показывает средний (20k)

### Проверьте на desktop:
1. ✅ Grid/Flex работает как раньше
2. ✅ Hover effects активны
3. ✅ Нет горизонтального скролла
4. ✅ Responsive переход 768px

---

## 📁 Измененные файлы

1. **Новые файлы:**
   - `/components/CorporateGiftReasonsTestPage.tsx` - Тестовая страница
   - `/MOBILE_HORIZONTAL_SCROLL_UPDATE.md` - Эта документация

2. **Обновленные файлы:**
   - `/App.tsx` - Добавлен route и navigation
   - `/components/DevNavigationButton.tsx` - Добавлена кнопка
   - `/components/B2BPackagesByPriceV2Variants.tsx` - Horizontal scroll + auto-center
   - `/components/CorporateB2BPage.tsx` - PackagingCTASection horizontal scroll
   - `/components/HowItWorksPage.tsx` - PackagingCTASection horizontal scroll
   - `/styles/globals.css` - Утилита scrollbar-hide

---

## 💡 Технические детали

### Auto-scroll на мобильных (только для пакетов):
- Использует `useRef` для доступа к DOM
- Проверяет ширину окна (`window.innerWidth < 768`)
- Вычисляет позицию среднего элемента
- Применяет smooth scroll при первом рендере

### Snap Scrolling:
- `snap-x snap-mandatory` - обязательный snap по X
- `snap-center` - snap к центру карточки
- Работает нативно без JS

### Responsive Breakpoint:
- `md:` = `768px` (Tailwind default)
- Mobile: `< 768px`
- Desktop: `≥ 768px`

---

## 🎯 Варианты "Подарок, который оценят"

### Variant 1: Well-being и HR-метрики 📊
- **Цвета:** Emerald-Teal
- **Фокус:** Измеримые бизнес-показатели
- **Метрики:** На каждой карточке
- **Для кого:** HR-директора, CFO

### Variant 2: Персонализация и Кейсы 🎁
- **Цвета:** Purple
- **Фокус:** Универсальность для событий
- **Badges:** Сезонное, Популярное, Премиум
- **Для кого:** Event-менеджеры, HR

### Variant 3: Минимализм с ROI 📈
- **Цвета:** Multi-color (Emerald, Blue, Teal, Amber)
- **Фокус:** Чистый дизайн + крупные цифры
- **Стиль:** Без фотографий, иконки
- **Для кого:** Топ-менеджеры, собственники

---

## ✨ Следующие шаги (опционально)

### Возможные улучшения:
1. 🔄 Swipe indicators (точки) на мобильных
2. 🎯 Touch feedback animations
3. 📊 Analytics tracking для свайпов
4. ⌨️ Keyboard navigation support
5. 🎨 Custom scrollbar дизайн (опционально)

---

## 📞 Поддержка

Если нужны дополнительные изменения:
- Изменить auto-scroll позицию (сейчас средний пакет)
- Изменить ширину карточек на мобильных (сейчас 85vw)
- Добавить/удалить snap behavior
- Настроить breakpoints

---

**Статус:** ✅ Готово к тестированию  
**Версия:** 1.0  
**Дата:** 5 ноября 2025
