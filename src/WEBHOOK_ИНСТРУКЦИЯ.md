# 🔗 Webhook для корпоративных заявок - Быстрая инструкция

## 🎯 Где менять URL webhook?

**Откройте файл:** `/config/webhooks.ts`

**Замените URL на свой:**
```typescript
export const CORPORATE_REQUEST_WEBHOOK_URL = 'https://your-webhook-url.com/api/corporate-requests';
```

**Вот и всё!** Теперь все заявки будут приходить на ваш адрес.

---

## 📦 Формат данных, который мы отправляем

### Пример полной заявки:

```json
{
  "name": "Иван Петров",
  "company": "ООО Рога и Копыта",
  "email": "ivan.petrov@company.ru",
  "phone": "+7 (912) 345-67-89",
  "employees": "150",
  "message": "Интересуют сертификаты для команды на новогодние корпоративы. Нужно 150 штук с брендированием.",
  "timestamp": "2025-11-07T14:30:00.000Z",
  "source": "corporate_modal",
  "userAgent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)...",
  "pageUrl": "https://глэмпинги.рф/corporate"
}
```

### Описание полей:

#### Обязательные (всегда присутствуют):
- **name** - Имя контактного лица
- **company** - Название компании
- **email** - Email для связи
- **phone** - Телефон в формате `+7 (xxx) xxx-xx-xx`
- **timestamp** - Дата и время отправки (ISO 8601)
- **source** - Откуда отправлена заявка (обычно `"corporate_modal"`)

#### Необязательные (могут отсутствовать):
- **employees** - Количество сотрудников (если указал пользователь)
- **message** - Пожелания и детали (если указал пользователь)
- **userAgent** - Информация о браузере
- **pageUrl** - Страница, откуда отправлена заявка

---

## 🧪 Как протестировать?

### Вариант 1: Использовать webhook.site (самый простой)

1. Откройте https://webhook.site
2. Скопируйте "Your unique URL" (например, `https://webhook.site/abc-123`)
3. Вставьте в `/config/webhooks.ts`:
   ```typescript
   export const CORPORATE_REQUEST_WEBHOOK_URL = 'https://webhook.site/abc-123';
   ```
4. Отправьте тестовую заявку через форму на сайте
5. Обновите страницу webhook.site - увидите данные!

### Вариант 2: Использовать свой тестовый сервер

Если у вас есть локальный сервер:

1. Запустите свой сервер (например, на порту 3000)
2. Установите ngrok: `brew install ngrok` (macOS) или скачайте с https://ngrok.com
3. Запустите: `ngrok http 3000`
4. Скопируйте URL из вывода (например, `https://abc123.ngrok.io`)
5. Вставьте в `/config/webhooks.ts`:
   ```typescript
   export const CORPORATE_REQUEST_WEBHOOK_URL = 'https://abc123.ngrok.io/api/webhook';
   ```

---

## 📊 Примеры данных для разных случаев

### Минимальная заявка (только обязательные поля):
```json
{
  "name": "Мария",
  "company": "ИП Иванов",
  "email": "maria@example.com",
  "phone": "+7 (999) 888-77-66",
  "timestamp": "2025-11-07T15:45:00.000Z",
  "source": "corporate_modal"
}
```

### Заявка с подробным описанием:
```json
{
  "name": "Алексей Смирнов",
  "company": "АО Северсталь",
  "email": "a.smirnov@severstal.com",
  "phone": "+7 (921) 111-22-33",
  "employees": "500",
  "message": "Рассматриваем варианты корпоративных подарков для 500 сотрудников. Интересует:\n- Сертификаты 25 000₽\n- Брендирование с логотипом\n- Доставка в Москве и СПб\n- Отсрочка платежа",
  "timestamp": "2025-11-07T16:20:00.000Z",
  "source": "corporate_modal",
  "userAgent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7)",
  "pageUrl": "https://глэмпинги.рф/corporate"
}
```

---

## 🖥️ Пример обработки на сервере (Node.js)

```javascript
const express = require('express');
const app = express();

app.use(express.json());

app.post('/api/webhook', (req, res) => {
  const data = req.body;
  
  console.log('📨 Новая заявка:');
  console.log('Компания:', data.company);
  console.log('Контакт:', data.name);
  console.log('Email:', data.email);
  console.log('Телефон:', data.phone);
  console.log('Сотрудников:', data.employees || 'не указано');
  console.log('Сообщение:', data.message || 'нет');
  
  // Здесь ваша логика:
  // - Сохранить в БД
  // - Отправить email
  // - Добавить в CRM
  
  res.json({ success: true });
});

app.listen(3000);
```

---

## ✅ Чеклист запуска

- [ ] Изменил URL в `/config/webhooks.ts`
- [ ] Убедился, что сервер доступен по этому URL
- [ ] Протестировал с webhook.site или локально
- [ ] Проверил, что данные приходят в нужном формате
- [ ] Настроил обработку на сервере (сохранение в БД, отправка email и т.д.)

---

## ❓ Проблемы?

**Заявки не приходят?**
1. Проверьте URL в `/config/webhooks.ts`
2. Откройте консоль браузера (F12 → Console)
3. Отправьте заявку
4. Ищите сообщения со значками 📤 ✅ или ❌

**Где смотреть логи?**
- В браузере: F12 → Console
- На сервере: ваши серверные логи

---

## 📁 Важные файлы

- `/config/webhooks.ts` - **ЗДЕСЬ МЕНЯТЬ URL**
- `/utils/webhookService.ts` - логика отправки данных
- `/components/CorporateRequestFormModal.tsx` - форма заявки
- `/WEBHOOK_INTEGRATION_GUIDE.md` - подробная документация (на английском)

---

**Готово! Удачи! 🚀**
