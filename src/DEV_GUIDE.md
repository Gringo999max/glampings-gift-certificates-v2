# 🛠️ Руководство разработчика - глэмпинги.рф

## 📁 Структура проекта

### Основные компоненты

```
/components
├── CheckoutModal.tsx        ← Главная форма заказа
├── Header.tsx              ← Шапка сайта
├── HeroSection.tsx         ← Главный баннер
├── HowItWorksSection.tsx   ← Блок "Как это работает"
├── GiftOptionsSection.tsx  ← Секция выбора сертификатов
└── PetFriendlyGlampingPage.tsx ← Страница глэмпинга с питомцем
```

### Утилиты

```
/components/utils
├── orderUtils.ts           ← Логика обработки заказов
└── testOrderUtils.ts       ← Тесты для orderUtils
```

---

## 🎠 Карусель упаковок и конвертов

### Где находится код?
**Файлы:**
- `/components/PackagingCarousel.tsx` - Компонент карусели упаковок
- `/components/EnvelopeCarousel.tsx` - Компонент карусели конвертов
- `/components/PackagingModal.tsx` - Модальное окно с выбором упаковки

### Как работает?

```typescript
// 1. Создаём расширенный массив для бесшовной прокрутки
const extendedOptions = [...options, ...options, ...options]

// 2. Рассчитываем стартовый индекс
const startIndex = options.length + currentIndex

// 3. Применяем transform с учётом gap
transform: `translateX(calc(-${startIndex * 33.333}% - ${startIndex * 0.75}rem))`
```

### Настройки

```typescript
// Скорость переключения (мс)
const CAROUSEL_INTERVAL = 1000

// Длительность анимации (мс)
const ANIMATION_DURATION = 1000

// Количество видимых элементов
const VISIBLE_ITEMS = 3
```

### Как добавить новый вариант упаковки?

```typescript
// В PackagingModal.tsx найдите массив packagingOptions (строка ~8)
const packagingOptions = [
  { 
    id: 'new-variant', 
    name: 'Новая упаковка', 
    gradient: 'from-blue-100 to-blue-200' 
  },
  // ... остальные варианты
]

// Также добавьте в CheckoutModal.tsx (строка ~57)
```

### Как добавить новый вариант конверта?

```typescript
// В PackagingModal.tsx найдите массив envelopeOptions (строка ~15)
const envelopeOptions = [
  { 
    id: 'new-color', 
    name: 'Новый цвет', 
    color: '#hexcolor',
    sealColor: '#hexcolor' 
  },
  // ... остальные варианты
]

// Также добавьте в CheckoutModal.tsx (строка ~64)
```

---

## 🦊 Блок "Как это работает"

### Где находится код?
**Файл:** `/components/HowItWorksSection.tsx`

### Импорт изображений

```typescript
import step1Image from 'figma:asset/473c627910d9537f1ff6b8d1db2964f79ea88c39.png'
import step2Image from 'figma:asset/bf412929b27600f04ec71124eb18e40eb9366774.png'
import step3Image from 'figma:asset/0c06a85ab403bb43c88e7bd4c6161dca48c59a1d.png'
```

### Как изменить текст шагов?

```typescript
const steps = [
  {
    number: 1,
    title: 'Заголовок шага 1',
    description: 'Описание шага 1',
    image: step1Image,
  },
  // ... остальные шаги
]
```

---

## 🎨 Цветовая схема

### Основные цвета

```css
/* Зелёный (основной) */
--emerald-50: rgb(236, 253, 245)
--emerald-100: rgb(209, 250, 229)
--emerald-500: rgb(16, 185, 129)
--emerald-600: rgb(5, 150, 105)
--emerald-700: rgb(4, 120, 87)

/* Фиолетовый (для PetFriendly) */
--purple-50: rgb(250, 245, 255)
--purple-600: rgb(147, 51, 234)

/* Ошибки */
--error-border: #ef4444
--error-bg: #fef2f2
--error-text: #f87171
```

### Где настраиваются?
**Файл:** `/styles/globals.css`

---

## 🚀 Производительность

### Lazy Loading

Страницы загружаются лениво для ускорения первой загрузки:

```typescript
const DeliveryPaymentPage = React.lazy(() => import('./components/DeliveryPaymentPage'))
const CorporatePage = React.lazy(() => import('./components/CorporatePage'))
// и т.д.
```

### Оптимизация изображений

```typescript
// Используйте ImageWithFallback для всех изображений
import { ImageWithFallback } from './components/figma/ImageWithFallback'

<ImageWithFallback
  src={imageUrl}
  alt="Описание"
  className="w-full h-full object-cover"
/>
```

### GPU-ускорение

```css
/* Добавьте класс для плавных анимаций */
.gpu-accelerated {
  transform: translateZ(0);
  backface-visibility: hidden;
  perspective: 1000px;
}
```

---

## 🐛 Отладка

### React DevTools

1. Установите расширение React Developer Tools
2. Откройте консоль разработчика
3. Перейдите на вкладку "⚛️ Components"

### Проверка состояния корзины

```javascript
// В консоли браузера:
console.log(window.__CART_STATE__)
```

### Логирование форм

```typescript
// В CheckoutModal.tsx раскомментируйте:
console.log('Form data:', formData)
console.log('Form errors:', formErrors)
console.log('Validation result:', validationResult)
```

---

## 📦 Сборка и деплой

### Локальная разработка

```bash
npm run dev
```

### Production сборка

```bash
npm run build
```

### Проверка типов

```bash
npm run type-check
```

---

## 🧪 Тестирование

### Тест карусели упаковок

1. Откройте модальное окно PackagingModal
2. Выберите "В подарочной упаковке и фирменном конверте"
3. Проверьте:
   - ✅ Карусель автоматически переключается каждую секунду
   - ✅ При наведении мыши карусель останавливается
   - ✅ При hover на варианте появляется zoom preview
   - ✅ Кнопки ← → работают корректно
   - ✅ Клик по индикатору переключает вручную
   - ✅ Выбор варианта работает корректно (без кнопки "Выбрать")
   - ✅ После выбора упаковки появляется карусель конвертов

### Тест формы валидации

1. Попытайтесь отправить пустую форму
2. Проверьте:
   - ✅ Красные рамки вокруг полей с ошибками
   - ✅ Текст ошибок под полями
   - ✅ Автоскролл к первому полю с ошибкой
   - ✅ Кнопка "Оплатить" показывает количество ошибок

---

## 📞 Контакты

**Проект:** глэмпинги.рф  
**Дата последнего обновления:** 3 октября 2025  
**Версия:** 2.0.0
